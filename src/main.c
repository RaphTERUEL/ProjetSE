#include "headers/header.h"

int main()
{
     disk mondisk;
 
   	init(&mondisk);
   	//root(&mondisk);
   	charger(&mondisk);
   	terminal(&mondisk);
   	sauvegarder(&mondisk);
   	
    return 0;
}
